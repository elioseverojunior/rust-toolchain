import{V as e,et as t,mt as n,tt as r}from"./chunks/framework.BkS4K3kz.js";var i=JSON.parse(`{"title":"Runbooks","description":"","frontmatter":{},"headers":[],"relativePath":"RUNBOOKS.md","filePath":"RUNBOOKS.md","lastUpdated":1785345004000}`),a={name:`RUNBOOKS.md`};function o(e,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[r(`<h1 id="runbooks" tabindex="-1">Runbooks <a class="header-anchor" href="#runbooks" aria-label="Permalink to “Runbooks”">​</a></h1><h2 id="development-setup" tabindex="-1">Development Setup <a class="header-anchor" href="#development-setup" aria-label="Permalink to “Development Setup”">​</a></h2><h3 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to “Prerequisites”">​</a></h3><ul><li><strong>mise</strong> — tool version manager (<code>brew install mise</code>)</li><li><strong>Docker</strong> — required by <code>act</code> for local workflow testing</li></ul><h3 id="first-time-setup" tabindex="-1">First-Time Setup <a class="header-anchor" href="#first-time-setup" aria-label="Permalink to “First-Time Setup”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> setup</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # Installs mise-managed tools and project dependencies</span></span></code></pre></div><p><code>setup</code> (aliases: <code>install</code>, <code>dev</code>, <code>dev:setup</code>, <code>dev:up</code>) runs <code>mise install</code>, <code>mise deps</code>, and <code>bun install</code> for you. To do it by hand:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">     # Managed tools (bun, gh, actionlint, …)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">      # Project dependencies</span></span></code></pre></div><h3 id="environment" tabindex="-1">Environment <a class="header-anchor" href="#environment" aria-label="Permalink to “Environment”">​</a></h3><p><code>GITHUB_TOKEN</code> is needed for <code>act</code> and <code>gh</code> operations:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> auth</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> login</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">        # Authenticate with GitHub</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gh:token</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    # Verify token works</span></span></code></pre></div><h2 id="development-workflow" tabindex="-1">Development Workflow <a class="header-anchor" href="#development-workflow" aria-label="Permalink to “Development Workflow”">​</a></h2><h3 id="tdd-cycle" tabindex="-1">TDD Cycle <a class="header-anchor" href="#tdd-cycle" aria-label="Permalink to “TDD Cycle”">​</a></h3><p>This project follows strict TDD:</p><ol><li>Write a failing test (<code>bun test</code> — <strong>red</strong>)</li><li>Write minimal code to pass (<strong>green</strong>)</li><li>Refactor while keeping tests passing</li></ol><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                    # Run tests once</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --watch</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">            # Watch mode for TDD</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/path/to/file.test.ts</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  # Single file</span></span></code></pre></div><h3 id="quality-gate" tabindex="-1">Quality Gate <a class="header-anchor" href="#quality-gate" aria-label="Permalink to “Quality Gate”">​</a></h3><p>Run before every commit — enforced by hk:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fix:all</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">      # ESLint fix + Prettier format</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    # tsc --noEmit (strict mode)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:coverage</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> # 100% line/function/statement gate</span></span></code></pre></div><h2 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to “Testing”">​</a></h2><h3 id="coverage-thresholds" tabindex="-1">Coverage Thresholds <a class="header-anchor" href="#coverage-thresholds" aria-label="Permalink to “Coverage Thresholds”">​</a></h3><p>Defined in <code>bunfig.toml</code>:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">coverageThreshold = { lines = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, statements = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>All three metrics must be <strong>100%</strong> per source file. Coverage excludes:</p><ul><li><code>dist/</code> — build output</li><li><code>bin/</code> — CLI bootstraps</li><li><code>__tests__/</code> — test infrastructure</li><li><code>integration-*/</code> — integration test suites</li></ul><h3 id="test-layout" tabindex="-1">Test Layout <a class="header-anchor" href="#test-layout" aria-label="Permalink to “Test Layout”">​</a></h3><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>src/</span></span>
<span class="line"><span>├── action.test.ts      # run() against injected fakes — argv, timeouts, retries, failures, outputs</span></span>
<span class="line"><span>├── core.test.ts        # TOML parsing, channel resolution, cache key, rustc parsing</span></span>
<span class="line"><span>├── config.test.ts      # Merge toml + inputs, path rejection, validation, resolveRustupEnv</span></span>
<span class="line"><span>├── builder.test.ts     # Fluent builder, ToolchainSpec, rustup argv generation</span></span>
<span class="line"><span>├── outputs.test.ts     # Resolved config → action outputs, JSON serialisation, provenance</span></span>
<span class="line"><span>├── lib.test.ts         # Barrel export surface, and that it never re-exports index.ts</span></span></code></pre></div><p><code>src/index.ts</code> has no co-located test: it is a side-effecting entry script, so importing it would run the action. Nothing imports it, so Bun never loads it and it does not appear in the coverage report — which is exactly why it holds only dependency wiring. All orchestration lives in <code>src/action.ts</code>, where <code>src/action.test.ts</code> drives it through the injected <code>ActionDeps</code>.</p><h2 id="building" tabindex="-1">Building <a class="header-anchor" href="#building" aria-label="Permalink to “Building”">​</a></h2><h3 id="build-the-action-bundle" tabindex="-1">Build the Action Bundle <a class="header-anchor" href="#build-the-action-bundle" aria-label="Permalink to “Build the Action Bundle”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build:action</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Output: dist/index.js</span></span></code></pre></div><p>This bundles <code>src/index.ts</code> with all dependencies into a single file for the GitHub Actions <code>node24</code> runtime (<code>runs.using</code> in <code>action.yml</code>).</p><p><code>dist/</code> is committed: GitHub fetches the action straight from the repository and needs the built bundle. The CI <strong>Build</strong> job runs <code>git diff --exit-code dist/</code>, so always rebuild and commit <code>dist/</code> alongside any <code>src/</code> change.</p><h2 id="local-github-action-testing-with-act" tabindex="-1">Local GitHub Action Testing with <code>act</code> <a class="header-anchor" href="#local-github-action-testing-with-act" aria-label="Permalink to “Local GitHub Action Testing with act”">​</a></h2><h3 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to “Setup”">​</a></h3><p>The <code>.actrc</code> file configures the local runner:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>-P=ubuntu-latest=catthehacker/ubuntu:full-latest</span></span>
<span class="line"><span>--pull=false</span></span>
<span class="line"><span>--container-daemon-socket=/var/run/docker.sock</span></span>
<span class="line"><span>--eventpath=./.act/event.json</span></span>
<span class="line"><span>--secret-file=./.act/.secrets</span></span>
<span class="line"><span>--var-file=./.act/.vars</span></span>
<span class="line"><span>--detect-event</span></span>
<span class="line"><span>--use-gitignore</span></span>
<span class="line"><span>--use-new-action-cache</span></span></code></pre></div><h3 id="run" tabindex="-1">Run <a class="header-anchor" href="#run" aria-label="Permalink to “Run”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> act</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">          # .github/workflows/tests/act.yml  — single case</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> act:matrix</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # .github/workflows/tests/act-matrix.yml — full matrix</span></span></code></pre></div><p>Both workflows exercise <strong>this</strong> action (<code>uses: ./</code>) end-to-end against a real rustup. GitHub does not run them: only <code>.github/workflows/*.yml</code> at the top level is picked up, and these live in a <code>tests/</code> subdirectory.</p><p>Each job:</p><ol><li>Checks out the repository</li><li>Creates an isolated <code>RUSTUP_HOME</code> (see the overlayfs note under Troubleshooting)</li><li>Optionally writes a <code>rust-toolchain.toml</code> for the case under test</li><li>Runs the action via <code>uses: ./</code></li><li>Asserts the <code>cachekey</code> and <code>name</code> outputs are non-empty</li><li>Asserts the isolated <code>RUSTUP_HOME</code> was honoured</li><li>Asserts the <strong>effective</strong> toolchain — <code>RUSTUP_TOOLCHAIN</code>, <code>rustup show active-toolchain</code>, and <code>rustc --version</code></li><li>Asserts requested targets, components, profile effects, and the default toolchain</li></ol><p>The matrix covers <code>stable</code>, <code>nightly</code>, a pinned <code>1.85.0</code>, a <code>&lt;major.minor&gt;</code> series from toml, toml-vs-input override precedence, and <code>profile: minimal</code>.</p><h3 id="act-directory" tabindex="-1"><code>.act/</code> Directory <a class="header-anchor" href="#act-directory" aria-label="Permalink to “.act/ Directory”">​</a></h3><table tabindex="0"><thead><tr><th>File</th><th>Purpose</th><th>Tracked in Git</th></tr></thead><tbody><tr><td><code>.act/README.md</code></td><td>Documentation</td><td>Yes</td></tr><tr><td><code>.act/event.json</code></td><td>Event payload for act</td><td>No</td></tr><tr><td><code>.act/.secrets</code></td><td>Secrets (e.g., GitHub_TOKEN)</td><td>No</td></tr><tr><td><code>.act/.vars</code></td><td>Variables for act</td><td>No</td></tr><tr><td><code>.act/.env</code></td><td>Environment variables</td><td>No</td></tr></tbody></table><h2 id="ci-cd-pipeline" tabindex="-1">CI/CD Pipeline <a class="header-anchor" href="#ci-cd-pipeline" aria-label="Permalink to “CI/CD Pipeline”">​</a></h2><h3 id="github-actions" tabindex="-1">GitHub Actions <a class="header-anchor" href="#github-actions" aria-label="Permalink to “GitHub Actions”">​</a></h3><p><code>.github/workflows/cicd.yml</code> runs on push, pull request, and <code>workflow_dispatch</code>, with these jobs:</p><table tabindex="0"><thead><tr><th>Job</th><th>Purpose</th></tr></thead><tbody><tr><td><code>setup</code></td><td>Checkout and provision the toolchain via mise</td></tr><tr><td><code>lint</code></td><td><code>hk</code> lint suite (ESLint, Prettier, actionlint, yamllint, markdownlint)</td></tr><tr><td><code>test</code></td><td><code>bun test</code> with the 100% coverage gate</td></tr><tr><td><code>sast</code></td><td>Static analysis / secret scanning</td></tr><tr><td><code>build</code></td><td>Rebuilds the bundle and fails on a stale <code>dist/</code> via <code>git diff --exit-code</code></td></tr><tr><td><code>release</code></td><td>Publishes the action on tagged releases</td></tr></tbody></table><p>Supporting workflows: <code>labeler.yml</code>, <code>stale-tags-cleanup.yml</code>, and <code>update-floating-tag.yml</code>.</p><h3 id="pre-commit-hooks-hk" tabindex="-1">Pre-Commit Hooks (hk) <a class="header-anchor" href="#pre-commit-hooks-hk" aria-label="Permalink to “Pre-Commit Hooks (hk)”">​</a></h3><p>Enforced before every commit:</p><ol><li><strong>commitlint</strong> — validates conventional commit format</li><li><strong>ESLint</strong> — strict mode, zero warnings</li><li><strong>Prettier</strong> — formatting check</li><li><strong>typecheck</strong> — <code>tsc --noEmit</code></li><li><strong>markdownlint</strong> — <code>rumdl</code></li><li><strong>mermaid</strong> — parses every <code>\`\`\`mermaid</code> block with mermaid&#39;s own parser</li><li><strong>actionlint</strong>, <strong>gitleaks</strong>, and the whitespace/EOF fixers</li><li><strong>coverage</strong> — <code>bun test --coverage</code> (100% gate), on <code>pre-commit:all</code></li></ol><h3 id="mermaid-diagrams" tabindex="-1">Mermaid diagrams <a class="header-anchor" href="#mermaid-diagrams" aria-label="Permalink to “Mermaid diagrams”">​</a></h3><p><code>rumdl</code> validates the fence, not the diagram inside it, so a syntax error would otherwise surface only as a broken render on GitHub. <code>scripts/lint-mermaid.ts</code> extracts every block and runs it through mermaid&#39;s own parser, reporting <code>file:line</code> on failure.</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mermaidlint</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">            # every Markdown file</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mermaidlint</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/x.md</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  # just one</span></span></code></pre></div><p>Mermaid 11 treats <code>@</code> as edge-id syntax, so a node label containing one must be quoted — <code>TK[&quot;@actions/core&quot;]</code>, not <code>TK[@actions/core]</code>.</p><h2 id="dependency-management" tabindex="-1">Dependency Management <a class="header-anchor" href="#dependency-management" aria-label="Permalink to “Dependency Management”">​</a></h2><h3 id="check-for-updates" tabindex="-1">Check for Updates <a class="header-anchor" href="#check-for-updates" aria-label="Permalink to “Check for Updates”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> deps:check</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    # List available minor updates</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> deps:latest</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # Update to latest versions</span></span></code></pre></div><h3 id="add-a-dependency" tabindex="-1">Add a Dependency <a class="header-anchor" href="#add-a-dependency" aria-label="Permalink to “Add a Dependency”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">packag</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">              # Production dependency</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dev</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">packag</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">        # Dev dependency</span></span></code></pre></div><p>Runtime dependencies are <code>@actions/core</code> (inputs, outputs, <code>exportVariable</code>, failures), <code>@actions/github</code> (workflow context), and <code>smol-toml</code> (TOML parsing).</p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to “Troubleshooting”">​</a></h2><h3 id="coverage-gate-fails" tabindex="-1">Coverage Gate Fails <a class="header-anchor" href="#coverage-gate-fails" aria-label="Permalink to “Coverage Gate Fails”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --coverage</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">           # See which file/metric fails</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> coverage/lcov.info</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">        # Inspect line coverage data</span></span></code></pre></div><p>Common causes:</p><ul><li><strong>Bun #7025</strong>: Classes with field declarations but no explicit constructor create a phantom function. Always add an explicit <code>constructor()</code>.</li><li><strong>New code</strong>: Ensure all functions, lines, and statements are covered.</li></ul><h3 id="act-fails" tabindex="-1"><code>act</code> Fails <a class="header-anchor" href="#act-fails" aria-label="Permalink to “act Fails”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Check Docker is running</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> info</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Verify secrets file exists</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -la</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .act/.secrets</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Regenerate GitHub token — the secret file is KEY=VALUE, not a bare token.</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># \`mise run act\` does this for you.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;GITHUB_TOKEN=$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> auth token)&quot;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .act/.secrets</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Run with verbose output</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">act</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -W</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .github/workflows/tests/act.yml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --verbose</span></span></code></pre></div><h3 id="invalid-cross-device-link-os-error-18-during-rustup-install" tabindex="-1"><code>Invalid cross-device link (os error 18)</code> during rustup install <a class="header-anchor" href="#invalid-cross-device-link-os-error-18-during-rustup-install" aria-label="Permalink to “Invalid cross-device link (os error 18) during rustup install”">​</a></h3><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>error: could not rename &#39;component&#39; file from</span></span>
<span class="line"><span>&#39;/home/runner/.rustup/toolchains/stable-.../share/doc/clippy&#39;</span></span>
<span class="line"><span>to &#39;/home/runner/.rustup/tmp/&lt;random&gt;_dir/bk&#39;: Invalid cross-device link (os error 18)</span></span></code></pre></div><p>This is an overlayfs limitation, not a rustup or toolchain-version problem. rustup renames a component&#39;s <strong>directory</strong> into <code>$RUSTUP_HOME/tmp</code> before replacing it, and overlayfs returns <code>EXDEV</code> for any directory rename whose source still lives in a lower image layer. Files are copied up on demand; directories are not, unless the mount uses <code>redirect_dir=on</code>. It therefore hits any toolchain baked into the container image — <code>stable</code> in <code>catthehacker/ubuntu:full-latest</code>, for example — while a freshly downloaded toolchain is unaffected.</p><p>Fix: point <code>RUSTUP_HOME</code> at a directory created at run time, so every rename stays inside the writable upper layer.</p><div class="language-yaml"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Prepare Isolated Rustup Home</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  shell</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">bash</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    rustup_home=&quot;\${RUNNER_TEMP:-/tmp}/rustup&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    mkdir -p &quot;$rustup_home&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    echo &quot;RUSTUP_HOME=$rustup_home&quot; &gt;&gt; &quot;$GITHUB_ENV&quot;</span></span></code></pre></div><p>Leave <code>CARGO_HOME</code> alone so the rustup binary and its proxies stay on <code>PATH</code>; they read <code>RUSTUP_HOME</code> from the environment. Set this in a step rather than a job-level <code>env:</code> block — the <code>runner</code> context is not available there.</p><h3 id="later-steps-run-a-different-toolchain-than-the-one-installed" tabindex="-1">Later steps run a different toolchain than the one installed <a class="header-anchor" href="#later-steps-run-a-different-toolchain-than-the-one-installed" aria-label="Permalink to “Later steps run a different toolchain than the one installed”">​</a></h3><p><code>rustup default</code> is only the <em>last</em> entry in rustup&#39;s override chain, so a <code>rust-toolchain.toml</code> in the workspace beats it. The action exports <code>RUSTUP_TOOLCHAIN</code> (precedence 2) to prevent this. To confirm what is actually active:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rustup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> active-toolchain</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # names the override source</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rustc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --version</span></span></code></pre></div><h3 id="is-not-a-valid-rustup-toolchain-name" tabindex="-1"><code>&quot;...&quot; is not a valid rustup toolchain name</code> <a class="header-anchor" href="#is-not-a-valid-rustup-toolchain-name" aria-label="Permalink to “&quot;...&quot; is not a valid rustup toolchain name”">​</a></h3><p><code>resolveChannel</code> rejected the channel before anything ran. Expected shapes are <code>stable</code>, <code>nightly</code>, <code>1.89.0</code>, <code>nightly-2025-01-01</code>, optionally with a host triple. The same guard applies to targets, components and profiles via <code>mergeConfig</code>, which reports which kind of value was rejected.</p><h3 id="input-set-rustup-toolchain-must-be-true-or-false" tabindex="-1"><code>Input \\</code>set-rustup-toolchain\` must be &quot;true&quot; or &quot;false&quot;\` <a class="header-anchor" href="#input-set-rustup-toolchain-must-be-true-or-false" aria-label="Permalink to “Input \\set-rustup-toolchain\` must be &quot;true&quot; or &quot;false&quot;\`”">​</a></h3><p>The input accepts only YAML booleans. A typo is rejected rather than being read as <code>false</code>, which would silently stop pinning the toolchain for later steps.</p><h3 id="rustup-was-installed-but-the-job-cannot-find-it" tabindex="-1">rustup was installed but the job cannot find it <a class="header-anchor" href="#rustup-was-installed-but-the-job-cannot-find-it" aria-label="Permalink to “rustup was installed but the job cannot find it”">​</a></h3><p><code>bootstrapRustup</code> adds <code>$CARGO_HOME/bin</code> to <code>PATH</code> through <code>core.addPath</code>, which only affects <strong>subsequent</strong> steps — never the step already running. Within the action itself, rustup is invoked through the environment it was installed into. If a later step still cannot find it, check that <code>CARGO_HOME</code> was not changed between steps.</p><h3 id="a-bare-1-9-installed-something-unexpected" tabindex="-1">A bare <code>1.9</code> installed something unexpected <a class="header-anchor" href="#a-bare-1-9-installed-something-unexpected" aria-label="Permalink to “A bare 1.9 installed something unexpected”">​</a></h3><p>It resolves to <code>1.90</code>, not the 2015 release. <code>scaleBareMinor</code> scales a truncated minor while the scaled value names a release that already exists. Write the full <code>1.9.0</code> if you genuinely want the old one.</p><h3 id="typescript-errors" tabindex="-1">TypeScript Errors <a class="header-anchor" href="#typescript-errors" aria-label="Permalink to “TypeScript Errors”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">           # Full check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tsc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --noEmit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --pretty</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">       # With colored output</span></span></code></pre></div><p>Common issues:</p><ul><li><code>verbatimModuleSyntax</code> requires <code>import type</code> for type-only imports</li><li><code>noUncheckedIndexedAccess</code> requires <code>!</code> assertions on array access</li></ul><h3 id="bun-test-runner-issues" tabindex="-1">Bun Test Runner Issues <a class="header-anchor" href="#bun-test-runner-issues" aria-label="Permalink to “Bun Test Runner Issues”">​</a></h3><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Clear V8 coverage cache</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -rf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> coverage/</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Verify bun version</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --version</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # Should be ^1.3.14</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Run single test file</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/core.test.ts</span></span></code></pre></div><h2 id="adding-a-new-feature" tabindex="-1">Adding a New Feature <a class="header-anchor" href="#adding-a-new-feature" aria-label="Permalink to “Adding a New Feature”">​</a></h2><ol><li><strong>Write test first</strong> — create or extend <code>src/*.test.ts</code></li><li><strong>Implement</strong> — write minimal code to pass</li><li><strong>Verify</strong> — <code>bun run test:coverage</code> must show 100%</li><li><strong>Typecheck</strong> — <code>bun run typecheck</code> must pass</li><li><strong>Lint</strong> — <code>bun run fix:all</code> must be clean</li><li><strong>Build</strong> — <code>bun run build:action</code>, and commit the regenerated <code>dist/</code></li></ol><p>A new library module must be added to the barrel, <code>src/lib.ts</code>, and to the export list <code>src/lib.test.ts</code> pins — that test fails until both are updated, which is deliberate. Never add <code>src/index.ts</code> to the barrel: it is the action entry point, exports nothing, and calls <code>run()</code> on import.</p><p>Import siblings as <code>@rust-toolchain/&lt;module&gt;</code>, never <code>./&lt;module&gt;</code> and never <code>@/&lt;module&gt;</code>. The package specifier is what a consumer maps, so it is the only form that resolves both here and in their project; <code>@/</code> is for tests only.</p><p>Put new orchestration in <code>src/action.ts</code>, never in <code>src/index.ts</code> — the entry script is excluded from coverage, so logic added there is untested by construction.</p><h2 id="release-process" tabindex="-1">Release Process <a class="header-anchor" href="#release-process" aria-label="Permalink to “Release Process”">​</a></h2><p>Releases are automatic. Every push to <code>main</code> runs the CICD <strong>Release</strong> job, which:</p><ol><li>Computes the next version with GitVersion</li><li>Rewrites <code>owner/repo/...@vX.Y.Z</code> references under <code>.github/</code></li><li>Tags and publishes a GitHub Release</li></ol><p>Do <strong>not</strong> bump <code>package.json</code> or create tags by hand — the job does both, and a hand-made tag collides with the one it creates. To release, merge to <code>main</code> with a conventional commit message; the bump level follows from the commit type.</p><p>Prereleases are skipped: the job exits early when GitVersion reports a prerelease label.</p>`,103)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};