import{$ as e,Ot as t,V as n,X as r,at as i,bt as a,et as o,gt as s,it as c,nt as l,rt as u}from"./chunks/framework.B-65WMaj.js";var d=JSON.parse(`{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"ARCHITECTURE.md","filePath":"ARCHITECTURE.md","lastUpdated":1785603968000}`),f={name:`ARCHITECTURE.md`};function p(n,d,f,p,m,h){let g=a(`Mermaid`);return s(),l(`div`,null,[d[0]||=u(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to “Architecture”">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to “Overview”">​</a></h2><p>A TypeScript library for reading <code>rust-toolchain.toml</code> and building <code>rustup</code> toolchain install commands, designed as a GitHub Action component. Inspired by <a href="https://github.com/dtolnay/rust-toolchain" target="_blank" rel="noreferrer">dtolnay/rust-toolchain</a>.</p><p><code>action.yml</code> invokes <code>dist/index.js</code> <strong>twice</strong> per job — once as <code>main</code>, once as <code>post</code> — and <code>src/index.ts</code> is the single entry point for both, dispatching on <code>STATE_isPost</code>:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>GitHub Actions Runner</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>  ┌─────┴────────┐</span></span>
<span class="line"><span>  │  action.yml  │  runs: node24, main AND post: dist/index.js</span></span>
<span class="line"><span>  └─────┬────────┘</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>  ┌─────┴───────────────────────┐</span></span>
<span class="line"><span>  │  src/index.ts               │  Wiring only — dispatches on</span></span>
<span class="line"><span>  │  STATE_isPost === &quot;true&quot;?   │  STATE_isPost, set unconditionally</span></span>
<span class="line"><span>  └───────┬──────────┬──────────┘  by the main phase&#39;s first line</span></span>
<span class="line"><span>          │ no        │ yes</span></span>
<span class="line"><span>          v            v</span></span>
<span class="line"><span>  ┌────────────────┐  ┌────────────────┐</span></span>
<span class="line"><span>  │ run(deps)      │  │ runPost(deps)  │</span></span>
<span class="line"><span>  │ src/action.ts  │  │ src/action.ts  │</span></span>
<span class="line"><span>  └──┬───┬───┬───┬─┘  └───────┬────────┘</span></span>
<span class="line"><span>     │   │   │   └─────┐      │</span></span>
<span class="line"><span>  ┌──┘   │   └───┐     │      v</span></span>
<span class="line"><span>  v      v       v     v  saveLayers, then</span></span>
<span class="line"><span>┌──────┐┌──────┐┌───────┐┌─────────┐  writeSummarySafely</span></span>
<span class="line"><span>│ core ││config││builder││ outputs │  (src/cache/lifecycle.ts,</span></span>
<span class="line"><span>└──┬───┘└──┬───┘└───┬───┘└────┬────┘   src/cache/summary.ts)</span></span>
<span class="line"><span>   │       │        │         │</span></span>
<span class="line"><span>   └───┬───┘        │         │</span></span>
<span class="line"><span>       v            v         v</span></span>
<span class="line"><span> ┌────────────┐┌──────────┐┌─────────────┐</span></span>
<span class="line"><span> │ resolve/   ││Toolchain ││ flat keys + │</span></span>
<span class="line"><span> │ merge/parse││Spec      ││ json output │</span></span>
<span class="line"><span> └────────────┘└────┬─────┘└─────────────┘</span></span>
<span class="line"><span>                    │</span></span>
<span class="line"><span>                    v</span></span>
<span class="line"><span>     rustup install, then</span></span>
<span class="line"><span>     resolveCacheLifecycle:</span></span>
<span class="line"><span>     restoreLayers (src/cache/lifecycle.ts),</span></span>
<span class="line"><span>     saveState(&quot;cache&quot;, plans+restored+budget)</span></span></code></pre></div><p><code>run</code> (the main phase) restores every enabled cache layer after the toolchain install and hands what it restored to the post phase through <code>saveState</code>; <code>runPost</code> (the post phase) reads that same state back through <code>getState</code> and saves whatever is left to save. See <a href="#the-two-entrypoints-and-the-state-handoff">The Two Entrypoints And The State Handoff</a> below.</p><p><code>src/lib.ts</code> sits outside this flow: it is the library barrel for programmatic consumers, never loaded by the action itself.</p><h2 id="module-dependency-graph" tabindex="-1">Module Dependency Graph <a class="header-anchor" href="#module-dependency-graph" aria-label="Permalink to “Module Dependency Graph”">​</a></h2>`,8),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-23`,graph:`graph%20TD%0A%20%20%20%20subgraph%20Entry%0A%20%20%20%20%20%20%20%20I%5Bsrc%2Findex.ts%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Orchestration%0A%20%20%20%20%20%20%20%20R%5Bsrc%2Faction.ts%5D%0A%20%20%20%20%20%20%20%20L%5Bsrc%2Flib.ts%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20%22Cache%20%E2%80%94%20src%2Fcache%2F%22%0A%20%20%20%20%20%20%20%20CIN%5Binputs.ts%5D%0A%20%20%20%20%20%20%20%20CLF%5Blifecycle.ts%5D%0A%20%20%20%20%20%20%20%20CSM%5Bsummary.ts%5D%0A%20%20%20%20%20%20%20%20CKY%5Bkeys.ts%5D%0A%20%20%20%20%20%20%20%20CLY%5Blayers.ts%5D%0A%20%20%20%20%20%20%20%20CEV%5Benv.ts%5D%0A%20%20%20%20%20%20%20%20CPA%5Bpaths.ts%5D%0A%20%20%20%20%20%20%20%20CBU%5Bbudget.ts%5D%0A%20%20%20%20%20%20%20%20CCL%5Bclient.ts%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Foundation%0A%20%20%20%20%20%20%20%20CORE%5Bsrc%2Fcore.ts%5D%0A%20%20%20%20%20%20%20%20CFG%5Bsrc%2Fconfig.ts%5D%0A%20%20%20%20%20%20%20%20BLD%5Bsrc%2Fbuilder.ts%5D%0A%20%20%20%20%20%20%20%20OUT%5Bsrc%2Foutputs.ts%5D%0A%20%20%20%20%20%20%20%20INP%5Bsrc%2Finputs.ts%5D%0A%20%20%20%20%20%20%20%20ERR%5Bsrc%2Ferrors.ts%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20External%0A%20%20%20%20%20%20%20%20TK%5B%22%40actions%2Fcore%22%5D%0A%20%20%20%20%20%20%20%20AC%5B%22%40actions%2Fcache%22%5D%0A%20%20%20%20end%0A%0A%20%20%20%20I%20--%3E%20R%0A%20%20%20%20I%20--%3E%20CBU%0A%20%20%20%20I%20--%3E%20TK%0A%20%20%20%20I%20--%3E%20AC%0A%0A%20%20%20%20R%20--%3E%20CIN%0A%20%20%20%20R%20--%3E%20CLF%0A%20%20%20%20R%20--%3E%20CSM%0A%20%20%20%20R%20--%3E%20CLY%0A%20%20%20%20R%20--%3E%20CPA%0A%20%20%20%20R%20--%3E%20CBU%0A%20%20%20%20R%20--%3E%20CCL%0A%20%20%20%20R%20--%3E%20CORE%0A%20%20%20%20R%20--%3E%20CFG%0A%20%20%20%20R%20--%3E%20BLD%0A%20%20%20%20R%20--%3E%20OUT%0A%20%20%20%20R%20--%3E%20INP%0A%20%20%20%20R%20--%3E%20ERR%0A%0A%20%20%20%20CIN%20--%3E%20CKY%0A%20%20%20%20CIN%20--%3E%20CLY%0A%20%20%20%20CIN%20--%3E%20CEV%0A%20%20%20%20CIN%20--%3E%20CPA%0A%20%20%20%20CIN%20--%3E%20CBU%0A%20%20%20%20CIN%20--%3E%20CORE%0A%20%20%20%20CIN%20--%3E%20INP%0A%20%20%20%20CIN%20--%3E%20OUT%0A%0A%20%20%20%20CKY%20--%3E%20CLY%0A%20%20%20%20CLY%20--%3E%20CFG%0A%20%20%20%20CLF%20--%3E%20CCL%0A%20%20%20%20CLF%20--%3E%20CLY%0A%20%20%20%20CLF%20--%3E%20CBU%0A%20%20%20%20CLF%20--%3E%20ERR%0A%20%20%20%20CSM%20--%3E%20CLF%0A%0A%20%20%20%20OUT%20--%3E%20CKY%0A%20%20%20%20OUT%20--%3E%20CLY%0A%20%20%20%20OUT%20--%3E%20CLF%0A%20%20%20%20OUT%20--%3E%20BLD%0A%20%20%20%20OUT%20--%3E%20CFG%0A%20%20%20%20OUT%20--%3E%20CORE%0A%0A%20%20%20%20INP%20--%3E%20OUT%0A%20%20%20%20BLD%20--%3E%20CFG%0A%20%20%20%20CFG%20--%3E%20CORE%0A%20%20%20%20CORE%20--%3E%20ERR%0A%0A%20%20%20%20L%20--%3E%20R%0A`})]),_:1})),d[1]||=u(`<p>Two edges carry most of the design. <code>src/index.ts</code> is the only module that touches <code>@actions/cache</code>: everything below it depends on the <code>CacheClient</code> interface in <code>src/cache/client.ts</code>, which is type-only, so the tests never load the real SDK. And <code>src/action.ts</code> fans out to the cache modules rather than the reverse — no cache module imports the orchestrator, which is what lets each one be tested against plain values.</p><p>The barrel <code>src/lib.ts</code> re-exports all sixteen library modules; only its edge to <code>src/action.ts</code> is drawn, since the other fifteen would say nothing but &quot;the barrel exports everything&quot;. It deliberately does <strong>not</strong> re-export <code>src/index.ts</code>, whose import would execute the action — <code>src/lib.test.ts</code> asserts that exclusion.</p><h2 id="data-flow" tabindex="-1">Data Flow <a class="header-anchor" href="#data-flow" aria-label="Permalink to “Data Flow”">​</a></h2>`,3),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-33`,graph:`flowchart%20LR%0A%20%20%20%20TOML%5Brust-toolchain.toml%5D%0A%20%20%20%20INPUTS%5BAction%20Inputs%5D%0A%0A%20%20%20%20subgraph%20Core%0A%20%20%20%20%20%20%20%20PARSER%5BparseRustToolchainToml%5D%0A%20%20%20%20%20%20%20%20RESOLVER%5BresolveChannel%20%2B%20scaleBareMinor%5D%0A%20%20%20%20%20%20%20%20RUSTC%5BparseRustcVersion%5D%0A%20%20%20%20%20%20%20%20CACHEKEY%5BgenerateCacheKey%5D%0A%20%20%20%20%20%20%20%20SPECKEY%5BgenerateSpecCacheKey%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Config%0A%20%20%20%20%20%20%20%20MERGE%5BmergeConfig%20%2B%20validation%5D%0A%20%20%20%20%20%20%20%20ENV%5BresolveRustupEnv%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Builder%0A%20%20%20%20%20%20%20%20BUILDER%5BToolchainSpecBuilder%5D%0A%20%20%20%20%20%20%20%20SPEC%5BToolchainSpec%5D%0A%20%20%20%20%20%20%20%20CMD%5BtoRustupInstallArgs%20%2F%20toRustupTargetAddArgs%20%2F%20toRustupComponentAddArgs%5D%0A%20%20%20%20end%0A%0A%20%20%20%20TOML%20--%3E%20PARSER%0A%20%20%20%20PARSER%20--%3E%7CToolchainTomlConfig%7C%20MERGE%0A%20%20%20%20INPUTS%20--%3E%7CToolchainInputs%7C%20MERGE%0A%0A%20%20%20%20MERGE%20--%3E%7CResolvedToolchain%7C%20RESOLVER%0A%20%20%20%20RESOLVER%20--%3E%20BUILDER%0A%20%20%20%20BUILDER%20--%3E%7Cfluent%20chaining%7C%20SPEC%0A%20%20%20%20SPEC%20--%3E%20CMD%0A%0A%20%20%20%20PROCENV%5Bprocess.env%5D%20--%3E%20ENV%0A%20%20%20%20ENV%20--%3E%7CRUSTUP_HOME%20%2F%20CARGO_HOME%7C%20CMD%0A%0A%20%20%20%20CMD%20--%3E%20RUSTC%0A%20%20%20%20RUSTC%20--%3E%20CACHEKEY%0A%20%20%20%20CACHEKEY%20--%3E%20OUTS%5BbuildActionOutputs%5D%0A%20%20%20%20CACHEKEY%20--%3E%20SPECKEY%0A%20%20%20%20SPEC%20--%3E%20SPECKEY%0A%20%20%20%20SPECKEY%20--%3E%20OUTS%0A%0A%20%20%20%20SPEC%20--%3E%7Cresolved%20config%7C%20OUTS%0A%20%20%20%20PARSER%20--%3E%7Ctoml%20provenance%7C%20OUTS%0A%20%20%20%20INPUTS%20--%3E%7Cinput%20provenance%7C%20OUTS%0A%20%20%20%20OUTS%20--%3E%7Cflat%20keys%20%2B%20json%7C%20OUT%5BAction%20outputs%5D%0A%0A%20%20%20%20CMD%20--%3E%7Cexports%20RUSTUP_TOOLCHAIN%7C%20STEPS%5BLater%20workflow%20steps%5D%0A`})]),_:1})),d[2]||=e(`p`,null,[c(`The cache lane hangs off `),e(`code`,null,`generateSpecCacheKey`),c(`: the same digest that keeps two toolchains' outputs apart is what keeps their `),e(`code`,null,`target/`),c(` directories apart.`)],-1),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-37`,graph:`flowchart%20LR%0A%20%20%20%20CINPUTS%5B%22cache-*%20inputs%22%5D%0A%20%20%20%20PROCENV2%5Bprocess.env%5D%0A%20%20%20%20SPECKEY2%5BgenerateSpecCacheKey%5D%0A%0A%20%20%20%20subgraph%20Derive%0A%20%20%20%20%20%20%20%20RESOLVE%5BreadCacheRequest%5D%0A%20%20%20%20%20%20%20%20LAYERS%5BparseCacheLayers%5D%0A%20%20%20%20%20%20%20%20ENVHASH%5BhashBuildEnv%5D%0A%20%20%20%20%20%20%20%20PATHS%5BparseWorkspaces%20%2B%20buildPaths%5D%0A%20%20%20%20%20%20%20%20KEYS%5BbuildLayerKey%5D%0A%20%20%20%20%20%20%20%20COUTS%5BbuildCacheOutputs%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Lifecycle%0A%20%20%20%20%20%20%20%20RESTORE%5BrestoreLayers%5D%0A%20%20%20%20%20%20%20%20SAVE%5BsaveLayers%5D%0A%20%20%20%20%20%20%20%20BUDGET%5BmeasurePaths%20%2B%20parseSize%5D%0A%20%20%20%20end%0A%0A%20%20%20%20CINPUTS%20--%3E%20RESOLVE%0A%20%20%20%20RESOLVE%20--%3E%20LAYERS%0A%20%20%20%20RESOLVE%20--%3E%20PATHS%0A%20%20%20%20PROCENV2%20--%3E%20ENVHASH%0A%0A%20%20%20%20LAYERS%20--%3E%20KEYS%0A%20%20%20%20ENVHASH%20--%3E%7CenvHash%2C%20build%20layer%20only%7C%20KEYS%0A%20%20%20%20SPECKEY2%20--%3E%7CspecCacheKey%2C%20build%20layer%20only%7C%20KEYS%0A%0A%20%20%20%20KEYS%20--%3E%7Ckey%20%2B%20restoreKeys%7C%20RESTORE%0A%20%20%20%20KEYS%20--%3E%20COUTS%0A%20%20%20%20PATHS%20--%3E%20RESTORE%0A%20%20%20%20RESTORE%20--%3E%7Cmain%20phase%7C%20CLIENT%5B%22CacheClient%20%E2%80%94%20%40actions%2Fcache%22%5D%0A%20%20%20%20RESTORE%20--%3E%7Cexact%20%2F%20partial%20%2F%20miss%7C%20COUTS%0A%20%20%20%20COUTS%20--%3E%20COUT%5B%22cache%20%2B%20cache-hit%20outputs%22%5D%0A%0A%20%20%20%20RESTORE%20--%3E%7CCachePhaseState%20via%20saveState%7C%20SAVE%0A%20%20%20%20PATHS%20--%3E%20BUDGET%0A%20%20%20%20BUDGET%20--%3E%7Cover%20budget%2C%20skip%20layer%7C%20SAVE%0A%20%20%20%20SAVE%20--%3E%7Cpost%20phase%2C%20exact%20hits%20skipped%7C%20CLIENT%0A%20%20%20%20SAVE%20--%3E%20SUMMARY%5BrenderSummary%5D%0A`})]),_:1})),d[3]||=u(`<p><code>readCacheRequest</code> and <code>buildCacheOutputs</code> are the seam: the main phase derives every layer&#39;s key and path set up front, publishes them through the <code>cache</code> output whether or not the lifecycle runs, and hands the same values to the post phase as <code>CachePhaseState</code>. A workflow that would rather drive <code>actions/cache</code> itself reads that output and ignores the rest.</p><h2 id="run-sequence" tabindex="-1">Run Sequence <a class="header-anchor" href="#run-sequence" aria-label="Permalink to “Run Sequence”">​</a></h2><p><code>run(deps)</code> in <code>src/action.ts</code>, the main phase, in order. Everything below the dashed line happens only after the toolchain is installed.</p>`,3),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-47`,graph:`flowchart%20TD%0A%20%20%20%20Z%5BreadCacheRequest%2C%20validate%20cache-*%20inputs%5D%20--%3E%20Y%5Bexport%20RUST_TOOLCHAIN_CACHE_ON_FAILURE%2C%3Cbr%2F%3EsaveState%20isPost%20%3D%20true%2C%20unconditionally%5D%0A%20%20%20%20Y%20--%3E%20A%5Bread%20rust-toolchain.toml%5D%0A%20%20%20%20A%20--%3E%20B%5Bmerge%20inputs%2C%20validate%5D%0A%20%20%20%20B%20--%3E%20C%5BresolveChannel%5D%0A%20%20%20%20C%20--%3E%20D%5Bbuild%20ToolchainSpec%5D%0A%20%20%20%20D%20--%3E%20E%7Brustup%20on%20PATH%3F%7D%0A%20%20%20%20E%20--%3E%7Cno%7C%20F%5BbootstrapRustup%3A%3Cbr%2F%3Ecurl%20rustup-init%2C%20run%20it%2C%20addPath%5D%0A%20%20%20%20E%20--%3E%7Cyes%7C%20G%0A%20%20%20%20F%20--%3E%20G%5Brustup%20toolchain%20install%3Cbr%2F%3E--profile%20--target%20--component%3Cbr%2F%3E--allow-downgrade%20--no-self-update%5D%0A%20%20%20%20G%20--%3E%20H%5Brustup%20target%20add%20--toolchain%5D%0A%20%20%20%20H%20--%3E%20I%5Brustup%20component%20add%20--toolchain%3Cbr%2F%3Erequested%20components%5D%0A%20%20%20%20I%20--%3E%20P%5Brustup%20component%20add%20--toolchain%3Cbr%2F%3Eprofile%20components%2C%20failure%20tolerated%5D%0A%20%20%20%20P%20--%3E%20J%5Brustup%20default%3Cbr%2F%3Efailure%20tolerated%5D%0A%20%20%20%20J%20--%3E%20K%5Bexport%20RUSTUP_TOOLCHAIN%3Cbr%2F%3Eunless%20set-rustup-toolchain%3A%20false%5D%0A%20%20%20%20K%20-.-%3E%20L%5Brustc%20--version%20--verbose%5D%0A%20%20%20%20L%20--%3E%20M%5BapplyCargoDefaults%3A%3Cbr%2F%3ECARGO_INCREMENTAL%2C%20CARGO_TERM_COLOR%2C%3Cbr%2F%3Eregistry%20protocol%2C%20http%20multiplexing%5D%0A%20%20%20%20M%20--%3E%20Q%5BresolveCacheLifecycle%2C%20when%20cache%20is%20on%3A%3Cbr%2F%3ErestoreLayers%20per%20enabled%20layer%2C%3Cbr%2F%3EsaveState%20cache%20%3D%20plans%20%2B%20restored%20%2B%20budget%5D%0A%20%20%20%20Q%20--%3E%20N%5BbuildActionOutputs%20%2B%20toOutputEntries%3A%3Cbr%2F%3Ecachekey%2C%20cachekey-full%2C%20name%2C%3Cbr%2F%3Etoolchain%2C%20targets%2C%20target%2C%20components%2C%3Cbr%2F%3Eprofile%2C%20set-rustup-toolchain%2C%20cache%2C%3Cbr%2F%3Ecache-hit%2C%20json%5D%0A`})]),_:1})),d[4]||=u(`<h2 id="post-phase-sequence" tabindex="-1">Post-Phase Sequence <a class="header-anchor" href="#post-phase-sequence" aria-label="Permalink to “Post-Phase Sequence”">​</a></h2><p><code>runPost(deps)</code>, invoked by <code>action.yml</code>&#39;s <code>post:</code> when <code>post-if</code> is true — <code>success()</code> or <code>cache-on-failure: true</code>. It sees none of the main phase&#39;s locals; everything it needs crossed through <code>saveState</code>/<code>getState</code>.</p>`,2),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-54`,graph:`flowchart%20TD%0A%20%20%20%20S0%5BgetState%20cache%5D%20--%3E%20S1%7Bstate%20present%3F%7D%0A%20%20%20%20S1%20--%3E%7Cno%2C%20caching%20was%20off%3Cbr%2F%3Eor%20run%20never%20got%20that%20far%7C%20S2%5Breturn%2C%20no-op%5D%0A%20%20%20%20S1%20--%3E%7Cyes%7C%20S3%5BJSON.parse%20plans%2C%20restored%2C%20budget%5D%0A%20%20%20%20S3%20--%3E%20S4%5BsaveLayers%2C%20one%20decision%20per%20layer%3A%3Cbr%2F%3Eskip%20on%20an%20exact%20restore%20hit%2C%3Cbr%2F%3Eskip%20when%20measure%20throws%2C%3Cbr%2F%3Eskip%20over%20cache-budget%2C%3Cbr%2F%3Eotherwise%20client.save%5D%0A%20%20%20%20S4%20--%3E%20S5%5BwriteSummarySafely%3A%3Cbr%2F%3ErenderSummary%20as%20the%20job%20summary%5D%0A%20%20%20%20S3%20-.throw%3A%20malformed%20state.-%3E%20S6%5Bcaught%2C%20core.warning%2C%3Cbr%2F%3Enever%20setFailed%5D%0A%20%20%20%20S4%20-.throw%3A%20unexpected%20failure.-%3E%20S6%0A%20%20%20%20S5%20-.throw%3A%20GITHUB_STEP_SUMMARY%20unset.-%3E%20S7%5Bcaught%20inside%20writeSummarySafely%2C%3Cbr%2F%3Ecore.warning%2C%20saves%20already%20kept%5D%0A`})]),_:1})),d[5]||=e(`p`,null,[c(`Every `),e(`code`,null,`rustup`),c(` step is an argv array executed without a shell, bounded by a timeout, and retried up to three times with 1s/2s backoff.`)],-1),d[6]||=e(`h2`,{id:`class-hierarchy`,tabindex:`-1`},[c(`Class Hierarchy `),e(`a`,{class:`header-anchor`,href:`#class-hierarchy`,"aria-label":`Permalink to “Class Hierarchy”`},`​`)],-1),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-61`,graph:`classDiagram%0A%20%20%20%20class%20ToolchainTomlConfig%20%7B%0A%20%20%20%20%20%20%20%20%2Bstring%20channel%3F%0A%20%20%20%20%20%20%20%20%2Bstring%5B%5D%20targets%3F%0A%20%20%20%20%20%20%20%20%2Bstring%20profile%3F%0A%20%20%20%20%20%20%20%20%2Bstring%5B%5D%20components%3F%0A%20%20%20%20%20%20%20%20%2Bstring%20path%3F%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20ToolchainInputs%20%7B%0A%20%20%20%20%20%20%20%20%2Bstring%20toolchain%3F%0A%20%20%20%20%20%20%20%20%2Bstring%20targets%3F%0A%20%20%20%20%20%20%20%20%2Bstring%20target%3F%0A%20%20%20%20%20%20%20%20%2Bstring%20components%3F%0A%20%20%20%20%20%20%20%20%2Bstring%20profile%3F%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20RustupEnv%20%7B%0A%20%20%20%20%20%20%20%20%2Bstring%20RUSTUP_HOME%0A%20%20%20%20%20%20%20%20%2Bstring%20CARGO_HOME%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20ResolvedToolchain%20%7B%0A%20%20%20%20%20%20%20%20%2Bstring%20channel%0A%20%20%20%20%20%20%20%20%2Bstring%5B%5D%20targets%0A%20%20%20%20%20%20%20%20%2Bstring%5B%5D%20components%0A%20%20%20%20%20%20%20%20%2Bstring%20profile%3F%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20RustcVersionInfo%20%7B%0A%20%20%20%20%20%20%20%20%2Bstring%20version%0A%20%20%20%20%20%20%20%20%2Bstring%20commitHash%0A%20%20%20%20%20%20%20%20%2Bstring%20commitDate%0A%20%20%20%20%20%20%20%20%2Bstring%20cacheKey%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20CacheKeySpec%20%7B%0A%20%20%20%20%20%20%20%20%2Bstring%20channel%0A%20%20%20%20%20%20%20%20%2Bstring%5B%5D%20targets%0A%20%20%20%20%20%20%20%20%2Bstring%5B%5D%20components%0A%20%20%20%20%20%20%20%20%2Bstring%20profile%3F%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20ActionDeps%20%7B%0A%20%20%20%20%20%20%20%20%2Bexec(file%2C%20args%2C%20opts)%20ExecResult%0A%20%20%20%20%20%20%20%20%2BreadFile(path)%20string%0A%20%20%20%20%20%20%20%20%2Bcore%3A%20getInput%2FsetOutput%2FsetFailed%2FexportVariable%2FaddPath%2Finfo%2FsaveState%2FgetState%2Fwarning%2Fsummary%0A%20%20%20%20%20%20%20%20%2Benv%3A%20Record~string%2C%20string~%0A%20%20%20%20%20%20%20%20%2Bplatform%3A%20string%0A%20%20%20%20%20%20%20%20%2Bsleep(ms)%20void%0A%20%20%20%20%20%20%20%20%2Bcache%3A%20CacheClient%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20ToolchainSpec%20%7B%0A%20%20%20%20%20%20%20%20%2Breadonly%20channel%3A%20string%0A%20%20%20%20%20%20%20%20%2Breadonly%20targets%3A%20string%5B%5D%0A%20%20%20%20%20%20%20%20%2Breadonly%20components%3A%20string%5B%5D%0A%20%20%20%20%20%20%20%20%2Breadonly%20profile%3F%3A%20string%0A%20%20%20%20%20%20%20%20%2Bconstructor(args%3A%20ResolvedToolchain)%0A%20%20%20%20%20%20%20%20%2BtoRustupInstallArgs()%3A%20string%5B%5D%0A%20%20%20%20%20%20%20%20%2BtoRustupTargetAddArgs()%3A%20string%5B%5D%20%7C%20null%0A%20%20%20%20%20%20%20%20%2BtoRustupComponentAddArgs()%3A%20string%5B%5D%20%7C%20null%0A%20%20%20%20%20%20%20%20%2BtoRustupProfileComponentAddArgs()%3A%20string%5B%5D%20%7C%20null%0A%20%20%20%20%20%20%20%20%2BtoRustupDefaultArgs()%3A%20string%5B%5D%0A%20%20%20%20%20%20%20%20%2BtoRustupInstallCommand()%3A%20string%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20ToolchainSpecBuilder%20%7B%0A%20%20%20%20%20%20%20%20-channel%3A%20string%0A%20%20%20%20%20%20%20%20-targets%3A%20string%5B%5D%0A%20%20%20%20%20%20%20%20-components%3A%20string%5B%5D%0A%20%20%20%20%20%20%20%20-profile%3A%20string%20%7C%20undefined%0A%20%20%20%20%20%20%20%20%2Bconstructor()%0A%20%20%20%20%20%20%20%20%2BwithChannel(channel)%3A%20this%0A%20%20%20%20%20%20%20%20%2BwithTargets(...targets)%3A%20this%0A%20%20%20%20%20%20%20%20%2BwithComponents(...components)%3A%20this%0A%20%20%20%20%20%20%20%20%2BwithProfile(profile)%3A%20this%0A%20%20%20%20%20%20%20%20%2Bbuild()%3A%20ToolchainSpec%0A%20%20%20%20%7D%0A%0A%20%20%20%20class%20CacheClient%20%7B%0A%20%20%20%20%20%20%20%20%2Brestore(paths%2C%20key%2C%20restoreKeys)%20Promise~string%20%7C%20undefined~%0A%20%20%20%20%20%20%20%20%2Bsave(paths%2C%20key)%20Promise~void~%0A%20%20%20%20%7D%0A%0A%20%20%20%20ToolchainInputs%20--%3E%20ResolvedToolchain%20%3A%20merged%20into%0A%20%20%20%20ToolchainSpec%20--%3E%20ResolvedToolchain%20%3A%20depends%20on%0A%20%20%20%20ToolchainSpecBuilder%20--%3E%20ToolchainSpec%20%3A%20produces%0A%20%20%20%20ToolchainSpecBuilder%20--%3E%20ToolchainTomlConfig%20%3A%20reads%0A%20%20%20%20ToolchainSpec%20--%3E%20CacheKeySpec%20%3A%20satisfies%0A%20%20%20%20ActionDeps%20--%3E%20ToolchainSpec%20%3A%20run()%20builds%0A%20%20%20%20ActionDeps%20--%3E%20CacheClient%20%3A%20restores%2Fsaves%20through%0A`})]),_:1})),d[7]||=u(`<h2 id="source-file-map" tabindex="-1">Source File Map <a class="header-anchor" href="#source-file-map" aria-label="Permalink to “Source File Map”">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Type</th><th>Exports</th><th>Responsibilities</th></tr></thead><tbody><tr><td><code>src/index.ts</code></td><td>Entry (node:child_process, node:fs)</td><td>none — side-effecting script</td><td>Dependency wiring only, dispatching on <code>STATE_isPost</code>: for the main phase, hands real <code>spawnSync</code>, <code>readFileSync</code>, <code>@actions/core</code> and a synchronous <code>sleep</code> to <code>run()</code>; for the post phase, hands the real <code>@actions/cache</code> client and a <code>node:fs</code>-backed <code>measure()</code> to <code>runPost()</code>. Invisible to the coverage gate — see <a href="#the-actionscache-adapter-lives-in-indexts">Cache Lifecycle</a></td></tr><tr><td><code>src/action.ts</code></td><td>Module</td><td><code>run</code>, <code>runPost</code>, <code>ActionDeps</code>, <code>PostDeps</code>, <code>ExecResult</code>, <code>ExecOptions</code></td><td>Main phase (<code>run</code>) — rustup bootstrap, install, targets, components, default, <code>RUSTUP_TOOLCHAIN</code>, cargo env defaults, cache-lifecycle restore, outputs. Post phase (<code>runPost</code>) — replays the state <code>run</code> saved through <code>saveState</code> and saves whatever is left to save. Argv only, timeouts, retries</td></tr><tr><td><code>src/core.ts</code></td><td>Module (smol-toml, node:crypto)</td><td><code>parseRustToolchainToml</code>, <code>resolveChannel</code>, <code>generateCacheKey</code>, <code>generateSpecCacheKey</code>, <code>parseRustcVersion</code>, types</td><td>TOML parsing, channel resolution/scaling/validation, cache key computation</td></tr><tr><td><code>src/config.ts</code></td><td>Module</td><td><code>mergeConfig</code>, <code>resolveRustupEnv</code>, <code>parseCommaList</code>, types</td><td>Merge toml config with action inputs (scalars replaced by inputs; lists accumulate deduped, inputs leading) and validate identifiers; resolve <code>RUSTUP_HOME</code>/<code>CARGO_HOME</code>, honouring caller-supplied values</td></tr><tr><td><code>src/builder.ts</code></td><td>Classes</td><td><code>ToolchainSpec</code>, <code>ToolchainSpecBuilder</code></td><td>Fluent builder pattern, rustup argv generation</td></tr><tr><td><code>src/outputs.ts</code></td><td>Module</td><td><code>buildActionOutputs</code>, <code>toOutputEntries</code>, <code>ActionOutputs</code>, <code>ActionOutputsArgs</code>, <code>BooleanInput</code>, <code>CacheOutputs</code>, <code>CacheLayerOutput</code>, provenance types</td><td>Maps the resolved spec plus its sources and the cache lifecycle&#39;s outcome onto the action&#39;s output surface; serialises lists as JSON arrays and the whole object as <code>json</code></td></tr><tr><td><code>src/errors.ts</code></td><td>Module</td><td><code>describeError</code></td><td>Renders a caught <code>unknown</code> as a message. Extracted because the <code>error instanceof Error ? error.message : String(error)</code> check was written out nine times across <code>action.ts</code>, <code>cache/lifecycle.ts</code> and <code>core.ts</code></td></tr><tr><td><code>src/inputs.ts</code></td><td>Module</td><td><code>readBooleanInput</code>, <code>InputReader</code></td><td>Shared YAML-boolean input parsing, used by both <code>action.ts</code> and <code>cache/inputs.ts</code> — belongs to neither on its own</td></tr><tr><td><code>src/cache/layers.ts</code></td><td>Module</td><td><code>CACHE_LAYER_IDS</code>, <code>CacheLayerId</code>, <code>parseCacheLayers</code></td><td>The canonical layer list (<code>registry</code>, <code>build</code>) and the <code>cache-layers</code> input parser</td></tr><tr><td><code>src/cache/keys.ts</code></td><td>Module</td><td><code>joinKeySegments</code>, <code>buildLayerKey</code>, <code>CacheKeyContext</code>, <code>CacheLayerKey</code></td><td>Per-layer key and restore-key ladder derivation; the <code>build</code> key folds in <code>envHash</code></td></tr><tr><td><code>src/cache/inputs.ts</code></td><td>Module</td><td><code>readCacheRequest</code>, <code>buildCacheOutputs</code>, <code>CacheRequest</code>, <code>CacheInputSource</code></td><td>Validates every <code>cache-*</code> input before anything installs (<code>readCacheRequest</code>, fails fast on a bad <code>cache-key-hash</code> or an oversized key); completes the validated request into per-layer keys once the spec digest exists (<code>buildCacheOutputs</code>)</td></tr><tr><td><code>src/cache/env.ts</code></td><td>Module (node:crypto)</td><td><code>hashBuildEnv</code></td><td>Digests the <code>CARGO_*</code>/<code>CC</code>/<code>CFLAGS</code>/<code>CXX</code>/<code>CMAKE</code>/<code>RUST*</code> environment into the <code>build</code> key&#39;s <code>envHash</code> segment, so jobs differing only in <code>RUSTFLAGS</code> stop sharing a key</td></tr><tr><td><code>src/cache/paths.ts</code></td><td>Module (node:path)</td><td><code>parseWorkspaces</code>, <code>registryPaths</code>, <code>buildPaths</code>, <code>Workspace</code></td><td>Reads <code>cache-workspaces</code> into resolved <code>&lt;manifest-dir&gt; -&gt; &lt;target-dir&gt;</code> mappings, refusing anything outside the checkout; the <code>registry</code> layer&#39;s fixed paths; the <code>build</code> layer&#39;s paths as a files-only glob set — <code>&lt;target&gt;/**</code> plus <code>!&lt;target&gt;/**/incremental/**</code>, <code>!&lt;target&gt;/**/examples/**</code> and the two directory negations that keep the manifest free of directories</td></tr><tr><td><code>src/cache/budget.ts</code></td><td>Module</td><td><code>parseSize</code>, <code>measurePaths</code>, <code>StatFs</code></td><td>Reads <code>cache-budget</code> into a byte count (binary suffixes, <code>0</code> disables it); sums a layer&#39;s on-disk size through an injected <code>StatFs</code> port</td></tr><tr><td><code>src/cache/client.ts</code></td><td>Module</td><td><code>CacheClient</code></td><td>The restore/save port. The only real implementation wraps <code>@actions/cache</code> and lives in <code>src/index.ts</code>, which nothing imports and the coverage gate does not measure</td></tr><tr><td><code>src/cache/lifecycle.ts</code></td><td>Module</td><td><code>restoreLayers</code>, <code>saveLayers</code>, <code>LayerPlan</code>, <code>RestoredLayer</code>, <code>SavedLayer</code>, <code>LayerResult</code>, <code>LifecycleLog</code>, <code>SaveArgs</code></td><td>Restores every enabled layer concurrently, downgrading any failure to a miss; decides whether each layer is worth saving — skip on an exact hit, skip when its size can&#39;t be measured, skip over budget — and saves the rest concurrently</td></tr><tr><td><code>src/cache/summary.ts</code></td><td>Module</td><td><code>renderSummary</code></td><td>Renders the per-layer restore/save outcome as the job summary&#39;s Markdown table</td></tr><tr><td><code>src/lib.ts</code></td><td>Barrel</td><td>re-exports <code>action</code>, <code>builder</code>, <code>cache/budget</code>, <code>cache/client</code>, <code>cache/env</code>, <code>cache/inputs</code>, <code>cache/keys</code>, <code>cache/layers</code>, <code>cache/lifecycle</code>, <code>cache/paths</code>, <code>cache/summary</code>, <code>config</code>, <code>core</code>, <code>errors</code>, <code>inputs</code>, <code>outputs</code> — never <code>index</code></td><td>The library surface under one specifier, for consumers that would rather import <code>@rust-toolchain</code> than sixteen modules</td></tr></tbody></table><p><code>src/lib.ts</code> is the barrel; individual modules can still be imported directly and are cheaper, since the barrel loads all sixteen.</p><h3 id="why-the-barrel-excludes-src-index-ts" tabindex="-1">Why the barrel excludes <code>src/index.ts</code> <a class="header-anchor" href="#why-the-barrel-excludes-src-index-ts" aria-label="Permalink to “Why the barrel excludes src/index.ts”">​</a></h3><p><code>src/index.ts</code> calls <code>run()</code> at the top level, so it is the one module in the tree whose import has a side effect — it shells out to <code>rustup</code>. Keeping it out of <code>src/lib.ts</code> is what makes that side effect unreachable to a consumer, and <code>src/lib.test.ts</code> asserts the exclusion in source rather than trusting a comment to survive.</p><h3 id="why-library-source-imports-itself-as-rust-toolchain" tabindex="-1">Why library source imports itself as <code>@rust-toolchain/*</code> <a class="header-anchor" href="#why-library-source-imports-itself-as-rust-toolchain" aria-label="Permalink to “Why library source imports itself as @rust-toolchain/*”">​</a></h3><p>Every module under <code>src/</code> imports its siblings through the package specifier (<code>import … from &quot;@rust-toolchain/config&quot;</code>), not through <code>./config</code> and not through the short <code>@/</code> alias. A path alias is a <em>consumer-visible</em> detail: when a sibling project maps <code>@rust-toolchain/*</code> at its own root, the library&#39;s internal imports resolve there too, because they use the very specifier the consumer mapped. An internal-only alias breaks that — a package whose source says <code>@/config</code> typechecks in its own repo and fails in every consumer with <code>TS2307: Cannot find module &#39;@/config&#39;</code>.</p><p><code>@/*</code> is mapped as a short form but confined to tests, which are never consumed. It must not appear in library source: a consumer almost certainly has their own <code>@/</code> pointing at their own <code>src</code>, so the import would resolve into their tree.</p><p>See <a href="https://github.com/elioseverojunior/rust-toolchain/blob/main/README.md#consuming-from-another-typescript-project" target="_blank" rel="noreferrer">README → Consuming from Another TypeScript Project</a>.</p><h2 id="build-pipeline" tabindex="-1">Build Pipeline <a class="header-anchor" href="#build-pipeline" aria-label="Permalink to “Build Pipeline”">​</a></h2>`,10),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-361`,graph:`flowchart%20LR%0A%20%20%20%20SRC%5Bsrc%2F*.ts%5D%0A%20%20%20%20TSC%5Btsc%20--noEmit%5D%0A%20%20%20%20BUN%5Bbun%20build%5D%0A%20%20%20%20DIST%5Bdist%2Findex.js%5D%0A%0A%20%20%20%20SRC%20--%3E%20TSC%0A%20%20%20%20SRC%20--%3E%20BUN%0A%20%20%20%20BUN%20--%3E%20DIST%0A%20%20%20%20DIST%20--%3E%20ACTION%5BGitHub%20Action%20Node24%20Runtime%5D%0A%0A%20%20%20%20subgraph%20Checks%0A%20%20%20%20%20%20%20%20LINT%5BESLint%5D%0A%20%20%20%20%20%20%20%20FORMAT%5BPrettier%5D%0A%20%20%20%20%20%20%20%20TEST%5Bbun%20test%20--coverage%5D%0A%20%20%20%20end%0A%0A%20%20%20%20SRC%20--%3E%20LINT%0A%20%20%20%20SRC%20--%3E%20FORMAT%0A%20%20%20%20SRC%20--%3E%20TEST%0A`})]),_:1})),d[8]||=u(`<h2 id="testing-strategy" tabindex="-1">Testing Strategy <a class="header-anchor" href="#testing-strategy" aria-label="Permalink to “Testing Strategy”">​</a></h2><p>All tests use Bun&#39;s built-in test runner with 100% coverage enforced by <code>bunfig.toml</code>.</p><table tabindex="0"><thead><tr><th>File</th><th>Tests</th><th>Coverage Target</th></tr></thead><tbody><tr><td><code>src/action.test.ts</code></td><td><code>run</code> against injected fakes — argv shape, timeouts, retry/backoff, spawn errors, rustup bootstrap (POSIX + Windows), cargo env defaults, tolerated <code>rustup default</code>, <code>set-rustup-toolchain</code>, outputs</td><td>100%</td></tr><tr><td><code>src/core.test.ts</code></td><td><code>parseRustToolchainToml</code>, <code>resolveChannel</code> (release-day table, bare-minor scaling, rejected names), <code>generateCacheKey</code>, <code>generateSpecCacheKey</code>, <code>parseRustcVersion</code></td><td>100% lines, functions, statements</td></tr><tr><td><code>src/config.test.ts</code></td><td><code>mergeConfig</code> — toml vs input priority, target alias, default channel, identifier validation; <code>resolveRustupEnv</code> — env overrides, blank handling, <code>HOME</code> fallback, Windows paths</td><td>100%</td></tr><tr><td><code>src/builder.test.ts</code></td><td><code>ToolchainSpecBuilder</code> fluent chain, <code>ToolchainSpec</code> direct construction, argv generation and batching, install flags</td><td>100%</td></tr><tr><td><code>src/outputs.test.ts</code></td><td><code>buildActionOutputs</code> — resolved values, empty-list and absent-profile edges, <code>inputs</code>/<code>toml</code> provenance; <code>toOutputEntries</code> — JSON array serialisation, string booleans, <code>json</code> key order</td><td>100%</td></tr><tr><td><code>src/cache/layers.test.ts</code></td><td><code>CACHE_LAYER_IDS</code>, <code>parseCacheLayers</code> — separators, dedup, unknown-layer and empty-selection rejection</td><td>100%</td></tr><tr><td><code>src/cache/keys.test.ts</code></td><td><code>joinKeySegments</code> — segment collapsing; <code>buildLayerKey</code> — registry vs. build key shape, restore-key ladders</td><td>100%</td></tr><tr><td><code>src/cache/env.test.ts</code></td><td><code>hashBuildEnv</code> — prefix matching, the exclusion deny-list, order independence, and stability across the action&#39;s own <code>exportVariable</code> calls</td><td>100%</td></tr><tr><td><code>src/cache/paths.test.ts</code></td><td><code>parseWorkspaces</code> — mapping syntax and escape rejection; <code>registryPaths</code>; <code>buildPaths</code> — the files-only manifest shape that makes the negation globs effective</td><td>100%</td></tr><tr><td><code>src/cache/budget.test.ts</code></td><td><code>parseSize</code> — binary suffixes and unparseable input; <code>measurePaths</code> — byte totals and the <code>unmeasured</code> fallback</td><td>100%</td></tr><tr><td><code>src/cache/inputs.test.ts</code></td><td><code>readCacheRequest</code> — every <code>cache-*</code> input, defaults and validation; <code>buildCacheOutputs</code> — the <code>cache</code> JSON object and <code>cache-hit</code></td><td>100%</td></tr><tr><td><code>src/cache/lifecycle.test.ts</code></td><td><code>restoreLayers</code> — exact/partial/miss classification; <code>saveLayers</code> — skip-on-exact-hit, budget refusal, per-layer failure isolation</td><td>100%</td></tr><tr><td><code>src/cache/summary.test.ts</code></td><td><code>renderSummary</code> — the job-summary table rendered from <code>CachePhaseState</code></td><td>100%</td></tr><tr><td><code>src/inputs.test.ts</code></td><td><code>readBooleanInput</code> — defaults, casing, and rejection of non-boolean text</td><td>100%</td></tr><tr><td><code>src/errors.test.ts</code></td><td><code>describeError</code> — <code>Error</code> instances and arbitrary thrown values</td><td>100%</td></tr><tr><td><code>src/lib.test.ts</code></td><td>Barrel surface pinned export by export, and the guard that <code>src/lib.ts</code> never re-exports <code>src/index.ts</code>; both path aliases resolve</td><td>100%</td></tr></tbody></table><p>Sixteen test files cover seventeen library modules. <code>src/cache/client.ts</code> declares the <code>CacheClient</code> interface and nothing else, so it compiles to no executable statements and never reaches the coverage report — the port exists precisely so the tests can drive the lifecycle without <code>@actions/cache</code>. <code>src/index.ts</code> is absent for the opposite reason: it is excluded through <code>coveragePathIgnorePatterns</code> because nothing imports it, which is why orchestration lives in <code>src/action.ts</code> behind the injected <code>ActionDeps</code>.</p><p>The release-cycle cases are pinned against real rust-lang.org release dates rather than against this codebase&#39;s own arithmetic, so a drifting epoch fails the suite instead of being reflected by it.</p><h2 id="key-design-decisions" tabindex="-1">Key Design Decisions <a class="header-anchor" href="#key-design-decisions" aria-label="Permalink to “Key Design Decisions”">​</a></h2><h3 id="toml-first-override-by-inputs" tabindex="-1">TOML-First, Override by Inputs <a class="header-anchor" href="#toml-first-override-by-inputs" aria-label="Permalink to “TOML-First, Override by Inputs”">​</a></h3><p>The action reads <code>rust-toolchain.toml</code> by default and merges it with action inputs. Inputs win on conflict. This mirrors the dtolnay/rust-toolchain behavior but adds explicit <code>profile</code> support.</p><h3 id="fluent-builder-pattern" tabindex="-1">Fluent Builder Pattern <a class="header-anchor" href="#fluent-builder-pattern" aria-label="Permalink to “Fluent Builder Pattern”">​</a></h3><p><code>ToolchainSpecBuilder</code> provides a fluent API with method chaining:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ToolchainSpecBuilder</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withChannel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stable&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withTargets</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wasm32-unknown-unknown&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withComponents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;clippy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rustfmt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withProfile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;minimal&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><h3 id="channel-resolution" tabindex="-1">Channel Resolution <a class="header-anchor" href="#channel-resolution" aria-label="Permalink to “Channel Resolution”">​</a></h3><p><code>resolveChannel</code> supports three expressive formats beyond literal channels:</p><ul><li><code>stable N (year|month|week|day) ago</code> — compute minor from date arithmetic</li><li><code>stable minus N releases</code> — subtract from current stable</li><li><code>1.XX</code> — passed through unchanged; rustup resolves a <code>&lt;major.minor&gt;</code> channel to the newest patch in that series</li></ul><h3 id="cache-key" tabindex="-1">Cache Key <a class="header-anchor" href="#cache-key" aria-label="Permalink to “Cache Key”">​</a></h3><p><code>generateCacheKey</code> produces a 12-character key from <code>date + commitHash</code> (first 12 chars), matching dtolnay/rust-toolchain&#39;s output format for cache compatibility. It is read through <code>RUSTUP_TOOLCHAIN</code>, so the key describes the toolchain that was requested rather than whatever a <code>rust-toolchain.toml</code> would have selected.</p><p>A <code>rustc</code> that cannot be run is fatal rather than silently yielding an empty key: every consumer keying a cache on an empty string would share one entry, with nothing in the log to explain it.</p><p><code>generateSpecCacheKey</code> publishes a second output, <code>cachekey-full</code>, appending a SHA-256 digest of the resolved channel, targets, components and profile. The compatible key describes the compiler build alone, so two jobs on the same rustc collide even when one installed <code>wasm32-unknown-unknown</code> and the other did not — and the second restores artifacts produced without its target. Targets and components are sorted before hashing, so the same set written in a different order still hits the same key. See <a href="./COMPARISON#cache-key">COMPARISON.md</a>.</p><h3 id="cache-layers" tabindex="-1">Cache Layers <a class="header-anchor" href="#cache-layers" aria-label="Permalink to “Cache Layers”">​</a></h3><p><code>cache: true</code> derives a key and restore-key ladder per cache layer, published through the <code>cache</code> output. Phase A shipped the keys; Phase B (below) restores and saves against them. Two layers exist today, defined in <code>src/cache/layers.ts</code> and keyed in <code>src/cache/keys.ts</code>:</p><ul><li><strong><code>registry</code></strong> — the downloaded crate sources under <code>~/.cargo/registry</code> and <code>~/.cargo/git</code>. Its key is <code>registry-&lt;os&gt;-&lt;arch&gt;-&lt;suffix&gt;-&lt;lockHash&gt;</code>, and it deliberately omits the resolved toolchain: any rustc can compile a source archive it did not build, so tying the key to the compiler would force a re-download on every toolchain bump for no benefit.</li><li><strong><code>build</code></strong> — the compiled artifacts under <code>target/</code>. Its key is <code>build-&lt;os&gt;-&lt;arch&gt;-&lt;suffix&gt;-&lt;specCacheKey&gt;-&lt;envHash&gt;-&lt;lockHash&gt;</code>, carrying the resolved spec and the digest of the build-affecting environment (<code>hashBuildEnv</code>, <code>src/cache/env.ts</code>) because those artifacts are both toolchain- and environment-specific. Its restore ladder stops one rung short of the bare <code>registry</code>-style prefix — the ladder is <code>build-&lt;os&gt;-&lt;arch&gt;-&lt;suffix&gt;-&lt;specCacheKey&gt;-&lt;envHash&gt;-</code> and nothing looser — because an entry built by a different toolchain or environment is not a useful restore: <code>cargo</code> would discard it on sight, and the run pays the download cost only to re-save it under a new key regardless.</li></ul><p>The layers are split by how often each one invalidates, which is the whole point of separating them: a <code>rustc</code> bump invalidates <code>build</code> but not <code>registry</code>, so splitting them stops a compiler upgrade from forcing a re-download of every crate it never touched.</p><p>The dependency-set hash (<code>cache-key-hash</code>) is a required input rather than something the action computes, because <code>hashFiles()</code> — the only thing that can glob a workspace&#39;s lockfiles — is a GitHub Actions workflow-expression function, unreachable from a Node action. Taking the workflow&#39;s own value also keeps these keys interoperable with a hash the workflow may already use elsewhere.</p><p><code>envHash</code> was added after the key format above already shipped, which means it changed every existing <code>build</code> key the moment it did: a workflow upgrading across that boundary sees exactly one cold <code>build</code> restore, then resumes hitting normally. See the upgrade note in <a href="https://github.com/elioseverojunior/rust-toolchain/blob/main/README.md#deriving-cargo-cache-keys-yourself" target="_blank" rel="noreferrer">README → Deriving cargo cache keys yourself</a>.</p><p><code>cache: true</code> restores and saves both layers itself — see <a href="#cache-lifecycle">Cache Lifecycle</a> below. A workflow can still derive the keys alone and wire its own <code>actions/cache</code> steps (see the <a href="https://github.com/elioseverojunior/rust-toolchain/blob/main/README.md#deriving-cargo-cache-keys-yourself" target="_blank" rel="noreferrer">README&#39;s key-only recipe</a>). See <a href="./design/2026-07-31-layered-cargo-cache"><code>docs/design/2026-07-31-layered-cargo-cache.md</code></a> for the full design rationale, including the <code>bin</code> layer that a later phase adds for cargo-installed tools.</p><h3 id="cache-lifecycle" tabindex="-1">Cache Lifecycle <a class="header-anchor" href="#cache-lifecycle" aria-label="Permalink to “Cache Lifecycle”">​</a></h3><p><code>resolveCacheLifecycle</code> in <code>src/action.ts</code> turns the derived keys into actual restores and saves, split across the main and post phases:</p><ul><li><strong>Restore</strong> (<code>restoreLayers</code>, <code>src/cache/lifecycle.ts</code>) runs every enabled layer&#39;s <code>client.restore</code> concurrently, in the main phase, right after the toolchain is installed — the <code>build</code> key needs <code>specCacheKey</code>, which does not exist until rustc has run. Any failure — a service outage, a reserved-key race — becomes a warning and an ordinary miss; a cache being unavailable must not fail a job that would otherwise succeed.</li><li><strong>Save</strong> (<code>saveLayers</code>/<code>saveLayer</code>, <code>src/cache/lifecycle.ts</code>) runs from the post phase, once the whole job has finished. Three independent reasons skip a layer: it matched its restore key exactly (unchanged, saving it again is pure budget burn); its size could not be measured (<code>measurePaths</code> threw — writing an entry of unknown size into a shared budget is the unsafe direction); or its measured size exceeds <code>cache-budget</code> (an oversized entry does not degrade its own hit rate, it evicts <em>other</em> workflows&#39; caches). Each layer&#39;s save is independently caught, so one layer&#39;s failure cannot lose the results of the others in the same batch.</li><li><strong>Exclusions are negation globs, never deletion — and they only work on a files-only manifest.</strong> <code>buildPaths</code> (<code>src/cache/paths.ts</code>) emits <code>&lt;target&gt;/**</code>, then <code>!&lt;target&gt;/**/incremental/**</code> and <code>!&lt;target&gt;/**/examples/**</code>, and finally <code>!&lt;target&gt;/</code> and <code>!&lt;target&gt;/**/</code>. Those last two are not decoration. <code>@actions/cache</code> resolves the patterns through <code>@actions/glob</code> with <code>implicitDescendants: false</code>, writes the matches to a manifest, and runs <code>tar --files-from &lt;manifest&gt;</code> — with no <code>--no-recursion</code>. Any directory left in the manifest is therefore expanded wholesale by tar and re-includes everything the negations removed, so excluding directories is what makes the other exclusions reach the archive. Verified at the tar layer, not only the glob layer: the pre-fix <code>[target, !target/*/incremental, …]</code> form resolved to the single entry <code>target</code>, from which tar archived the whole tree. The cost of a files-only manifest is that empty directories and directory permissions and mtimes are not preserved, which cargo does not depend on — it decides freshness from file mtimes and recreates any directory it needs. Nothing on disk is touched, so a save failure leaves the working tree exactly as it was. <code>registryPaths</code> keeps naming bare directories, and that asymmetry is deliberate: it carries no exclusions, so tar&#39;s recursion is exactly what archives it. <code>registry/src</code> is excluded there by never being listed at all, since it is fully regenerable from the <code>.crate</code> files in <code>registry/cache</code>.</li><li><strong>A cache failure never fails the build.</strong> Restore, save, size measurement and the job summary write are each wrapped so their own failure produces a <code>core.warning</code> and lets everything else continue — including <code>runPost</code>&#39;s outer <code>try</code>/<code>catch</code> around a malformed state payload, and <code>writeSummarySafely</code>&#39;s own inner one around a summary write that throws when <code>GITHUB_STEP_SUMMARY</code> is unset (slim runners, <code>act</code>).</li><li><strong>The job summary</strong> (<code>renderSummary</code>, <code>src/cache/summary.ts</code>) is the only place a per-layer result is visible — <code>cache-hit</code> is a single all-layers boolean, so telling a cold run from a broken key needs the table naming each layer&#39;s actual restore result and save outcome.</li></ul><h3 id="the-two-entrypoints-and-the-state-handoff" tabindex="-1">The Two Entrypoints And The State Handoff <a class="header-anchor" href="#the-two-entrypoints-and-the-state-handoff" aria-label="Permalink to “The Two Entrypoints And The State Handoff”">​</a></h3><p><code>action.yml</code> declares both <code>main</code> and <code>post</code> as <code>dist/index.js</code> — the same bundle, invoked twice. <code>src/index.ts</code> is the only place that tells the two invocations apart, by checking <code>process.env.STATE_isPost</code>, which GitHub Actions populates from whatever the main phase&#39;s <code>saveState(&quot;isPost&quot;, ...)</code> wrote.</p><p>That call is the first thing <code>run</code> does — unconditionally, before reading any cache input, before installing anything — specifically so a caching-<strong>disabled</strong> job&#39;s post invocation can still tell it is the post phase. Gating it on <code>cache: true</code> was tried and reverted: on the default configuration <code>STATE_isPost</code> would never be set, the post invocation would fall through to <code>run</code>&#39;s own branch in <code>src/index.ts</code>, and the whole toolchain install would run a second time — capable of turning an already-succeeded job into a failed one. <code>runPost</code> already no-ops correctly on empty state, which is what makes the unconditional <code>saveState</code> safe for every configuration.</p><p>The second piece of state, <code>&quot;cache&quot;</code>, crosses the same boundary as a JSON-encoded string: <code>{ plans, restored, budget }</code>, written by <code>resolveCacheLifecycle</code> only when caching is enabled, and read back by <code>runPost</code> through <code>getState(&quot;cache&quot;)</code>. An empty value there — caching was off, or the run never reached the lifecycle — is <code>runPost</code>&#39;s own no-op signal, independent of <code>isPost</code>. Because the two phases are separate process invocations, nothing else survives between them: no closures, no module-level state, only what was explicitly written to and read from these two keys.</p><h3 id="the-actions-cache-adapter-lives-in-index-ts" tabindex="-1">The <code>@actions/cache</code> Adapter Lives In <code>index.ts</code> <a class="header-anchor" href="#the-actions-cache-adapter-lives-in-index-ts" aria-label="Permalink to “The @actions/cache Adapter Lives In index.ts”">​</a></h3><p><code>CacheClient</code> (<code>src/cache/client.ts</code>) is a two-method port — <code>restore</code>/<code>save</code> — with no real implementation anywhere under <code>src/</code>. The only one wraps <code>@actions/cache</code> and is built inline in <code>src/index.ts</code>, which nothing imports and the coverage gate does not measure (see <code>CLAUDE.md</code> → Coverage gate gotchas). That placement is deliberate, not an oversight: <code>@actions/cache</code> vendors the Azure Storage SDK — most of the roughly 1.4 MB this feature added to <code>dist/index.js</code> — plus genuinely unmockable network code. A library module importing it directly would pull that weight, and that network code, into every test process, making the 100% coverage gate unreachable for anything downstream of it. Every module that needs to restore or save takes a <code>CacheClient</code> as a dependency instead, so tests inject a fake and the real adapter is exercised only by the actual GitHub Actions runtime — and by CI&#39;s <code>E2E</code> and <code>E2E Warm Cache</code> jobs, the only place restore and save meet the real cache service. It takes two jobs rather than one: <code>post:</code> runs after every other step in its own job, so nothing a job saves is visible to that same job, and the warm restore has to happen in a dependent job (see <a href="https://github.com/elioseverojunior/rust-toolchain/blob/main/README.md#built-to-be-trusted" target="_blank" rel="noreferrer">README → Built to be trusted</a>).</p><h3 id="toolchain-pinning" tabindex="-1">Toolchain Pinning <a class="header-anchor" href="#toolchain-pinning" aria-label="Permalink to “Toolchain Pinning”">​</a></h3><p>rustup resolves the active toolchain through an <a href="https://rust-lang.github.io/rustup/overrides.html" target="_blank" rel="noreferrer">override chain</a> — highest first: <code>+toolchain</code> shorthand, <code>RUSTUP_TOOLCHAIN</code>, directory override, <code>rust-toolchain.toml</code>, default. Three consequences shape this codebase:</p><ul><li><strong><code>src/action.ts</code> exports <code>RUSTUP_TOOLCHAIN</code></strong> after installing. Setting <code>rustup default</code> alone is not enough: a workspace <code>rust-toolchain.toml</code> outranks the default, so later steps would run a different toolchain than the one installed.</li><li><strong><code>src/builder.ts</code> pins <code>--toolchain</code> on every <code>target add</code> / <code>component add</code>.</strong> Without it those commands resolve through the same chain and attach targets or components to the toml&#39;s toolchain.</li><li><strong>A <code>path</code> toolchain is rejected.</strong> <code>path</code> selects a local directory and is mutually exclusive with <code>channel</code>, so there is nothing to install; <code>mergeConfig</code> throws rather than silently falling back to <code>stable</code>.</li></ul><h3 id="profiles-are-always-explicit" tabindex="-1">Profiles Are Always Explicit <a class="header-anchor" href="#profiles-are-always-explicit" aria-label="Permalink to “Profiles Are Always Explicit”">​</a></h3><p>Omitting <code>--profile</code> makes rustup fall back to the globally configured profile (<code>rustup set profile</code>), not to <code>default</code>. The <a href="https://rust-lang.github.io/rustup/overrides.html" target="_blank" rel="noreferrer">rustup book</a> says so directly about the toolchain file&#39;s <code>profile</code> key:</p><blockquote><p>Note that if not specified, the <code>default</code> profile is not necessarily used, as a different default profile might have been set with <code>rustup set profile</code>.</p></blockquote><p>Since <code>mergeConfig</code> resolves a profile for every run, <code>ToolchainSpec</code> always passes it explicitly so a runner-wide setting cannot change the result.</p><p><code>mergeConfig</code> accepts only the three profiles rustup defines — <code>minimal</code>, <code>default</code>, <code>complete</code> — narrowing the value to a <code>RustupProfile</code> union rather than passing an arbitrary string through. A typo is reported with the valid options listed instead of reaching rustup.</p><p>When neither the toml nor an input names one, the resolved value is <code>DEFAULT_PROFILE</code>, which is <strong><code>default</code></strong> — the same profile rustup itself defaults to (<code>minimal</code> plus <code>rust-docs</code>, <code>rustfmt</code> and <code>clippy</code>).</p><h3 id="the-profile-only-applies-to-a-fresh-toolchain" tabindex="-1">The Profile Only Applies to a Fresh Toolchain <a class="header-anchor" href="#the-profile-only-applies-to-a-fresh-toolchain" aria-label="Permalink to “The Profile Only Applies to a Fresh Toolchain”">​</a></h3><p>rustup silently ignores <code>--profile</code> when the toolchain is already installed. Verified on a clean <code>rust:trixie</code> container:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>fresh  install --profile minimal   → installed   cargo, rust-std, rustc</span></span>
<span class="line"><span>re-install     --profile default   → unchanged   cargo, rust-std, rustc</span></span>
<span class="line"><span>re-install     --profile complete  → unchanged   cargo, rust-std, rustc</span></span>
<span class="line"><span>re-install     --profile default --force</span></span>
<span class="line"><span>                                   → unchanged   cargo, rust-std, rustc</span></span>
<span class="line"><span>remove, then fresh --profile default</span></span>
<span class="line"><span>                                   → installed   cargo, clippy, rust-docs,</span></span>
<span class="line"><span>                                                 rust-std, rustc, rustfmt</span></span></code></pre></div><p>There is no rustup flag that applies a profile to an existing toolchain — <code>--force</code> does not do it. Explicitly named components <em>do</em> still install, which is why the action adds <code>components</code> through separate <code>rustup component add</code> calls rather than relying on the install flags.</p><h3 id="complete-is-not-installable-on-release-channels" tabindex="-1"><code>complete</code> Is Not Installable on Release Channels <a class="header-anchor" href="#complete-is-not-installable-on-release-channels" aria-label="Permalink to “complete Is Not Installable on Release Channels”">​</a></h3><p>The <code>complete</code> profile pulls in <code>miri</code> and <code>rustc-codegen-cranelift</code>, which rustup publishes only for nightly. On a fresh install against any release channel it fails outright — verified for both a pinned version and <code>stable</code>:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>rustup toolchain install --profile complete 1.95</span></span>
<span class="line"><span>  error: some components are unavailable for download for channel &#39;1.95&#39;:</span></span>
<span class="line"><span>         &#39;miri&#39; for target &#39;x86_64-unknown-linux-gnu&#39;,</span></span>
<span class="line"><span>         &#39;rustc-codegen-cranelift&#39; for target &#39;x86_64-unknown-linux-gnu&#39;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>rustup component list --toolchain 1.95    # what release channels actually offer</span></span>
<span class="line"><span>  llvm-tools, rust-analyzer, rust-src     # no miri, no cranelift</span></span></code></pre></div><p>The failure is deterministic, so retrying cannot help. <code>assertProfileAvailable</code> therefore rejects <code>complete</code> up front for any channel that is not nightly, before a single rustup command runs — turning a slow, opaque network failure into an immediate message naming the cause and the two ways out. The check runs on the <em>resolved</em> channel, so <code>stable 2 releases ago</code> is judged as the release it becomes.</p><p><code>beta</code> is rejected along with the release channels: a fresh container lists neither component for it.</p><p>On an already-installed toolchain <code>complete</code> appears to succeed purely because the profile is ignored (see above) — nothing is resolved, so nothing can be missing. That is why the check is on the channel rather than on the outcome.</p><p>Because <code>complete</code> requests the widest set, <code>--allow-downgrade</code> is added for it on <code>nightly</code> — the same latitude explicitly named components get, letting rustup step back to the newest nightly that carries everything.</p><h3 id="profiles-are-expanded-into-components" tabindex="-1">Profiles Are Expanded Into Components <a class="header-anchor" href="#profiles-are-expanded-into-components" aria-label="Permalink to “Profiles Are Expanded Into Components”">​</a></h3><p>Because rustup will not apply a profile to an existing toolchain, the action adds the profile&#39;s components by name after installing. <code>PROFILE_COMPONENTS</code> in <code>src/config.ts</code> holds the mapping:</p><table tabindex="0"><thead><tr><th>Profile</th><th>Added explicitly</th></tr></thead><tbody><tr><td><code>minimal</code></td><td>nothing — rustc, cargo and rust-std are inherent to any toolchain</td></tr><tr><td><code>default</code></td><td><code>rust-docs</code>, <code>rustfmt</code>, <code>clippy</code></td></tr><tr><td><code>complete</code></td><td>the above plus <code>rust-src</code>, <code>rust-analyzer</code>, <code>llvm-tools</code></td></tr></tbody></table><p><code>complete</code> deliberately omits <code>miri</code> and <code>rustc-codegen-cranelift</code>. rustup publishes them for nightly only, so naming them would fail the add on every release channel; they still arrive through <code>--profile complete</code> on a fresh nightly install.</p><p>The two component invocations carry different weight:</p><ul><li><strong>Requested components</strong> (<code>components:</code> input or toml) are a hard requirement. A failure fails the step.</li><li><strong>Profile components</strong> are best-effort. They were implied rather than asked for, so a channel that does not publish one is logged and the run continues.</li></ul><p>Components the caller already listed are filtered out, so nothing is requested twice.</p><p>Together with <code>default</code> as the default profile, this means a job on a runner with Rust pre-installed still ends up with rustfmt and clippy — which is what the profile promised and what rustup alone would not have delivered.</p><h3 id="bootstrapping-rustup" tabindex="-1">Bootstrapping rustup <a class="header-anchor" href="#bootstrapping-rustup" aria-label="Permalink to “Bootstrapping rustup”">​</a></h3><p>Self-hosted runners and slim container images frequently ship without rustup. If <code>rustup --version</code> does not answer, <code>bootstrapRustup</code> downloads <code>rustup-init</code> (<code>sh.rustup.rs</code>, or <code>win.rustup.rs/&lt;arch&gt;</code> on Windows) to <code>$RUNNER_TEMP</code>, runs it with <code>--default-toolchain none</code>, and puts <code>$CARGO_HOME/bin</code> on <code>PATH</code>. The installer is written to a file and executed directly rather than piped into a shell.</p><h3 id="cargo-environment-defaults" tabindex="-1">Cargo Environment Defaults <a class="header-anchor" href="#cargo-environment-defaults" aria-label="Permalink to “Cargo Environment Defaults”">​</a></h3><p>After the toolchain is installed, <code>applyCargoDefaults</code> sets what a Rust CI job almost always wants, and never overwrites a value the workflow set itself:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Value</th><th>Why</th></tr></thead><tbody><tr><td><code>CARGO_INCREMENTAL</code></td><td><code>0</code></td><td>Incremental artifacts never survive to the next run; producing them only costs time and cache space</td></tr><tr><td><code>CARGO_TERM_COLOR</code></td><td><code>always</code></td><td>Colour in the job log</td></tr><tr><td><code>CARGO_REGISTRIES_CRATES_IO_PROTOCOL</code></td><td><code>sparse</code> on 1.68–1.69, <code>git</code> on 1.66–1.67</td><td>The sparse registry was implemented in 1.66, stabilised in 1.68 and made default in 1.70, so only the versions in between need steering</td></tr><tr><td><code>CARGO_HTTP_MULTIPLEXING</code></td><td><code>false</code> on 1.70–1.71</td><td>Those toolchains bundle curl 8.0, which produced spurious network errors with multiplexing on</td></tr></tbody></table><h3 id="anchoring-the-release-cycle" tabindex="-1">Anchoring the Release Cycle <a class="header-anchor" href="#anchoring-the-release-cycle" aria-label="Permalink to “Anchoring the Release Cycle”">​</a></h3><p><code>stable N ago</code> and <code>stable minus N releases</code> count six-week cycles from 2015-05-14 — the same day dtolnay/rust-toolchain uses. The anchor is load-bearing in both directions: a week late names a release that has been superseded, and a week early names one that has not shipped, so <code>rustup toolchain install</code> fails outright. <code>stableMinorAtDate</code> works purely in absolute epoch milliseconds and <code>setUTC*</code> accessors, so the result never depends on the runner&#39;s timezone.</p><p>A bare <code>1.N</code> is scaled by <code>scaleBareMinor</code> while the scaled value names a release that already exists: <code>1.9</code> reads as <code>1.90</code>, <code>1.62</code> is left alone. Without it, writing <code>1.9</code> silently pins the 2015 release.</p><h3 id="argv-never-a-shell" tabindex="-1">Argv, Never a Shell <a class="header-anchor" href="#argv-never-a-shell" aria-label="Permalink to “Argv, Never a Shell”">​</a></h3><p>Channel, targets, components and profile can all originate in a <code>rust-toolchain.toml</code> belonging to the checked-out workspace, which on <code>pull_request_target</code> or any untrusted checkout is attacker-controlled. Every command is therefore built as an argv array by <code>ToolchainSpec</code> and executed without a shell, so a value like <code>stable; curl … | sh</code> reaches rustup as one (invalid) toolchain name instead of a second command.</p><p>Two validation layers back this up: <code>resolveChannel</code> rejects anything that cannot name a toolchain, and <code>mergeConfig</code> rejects targets, components and profiles that are not plain rustup identifiers. Neither is load-bearing on its own — they turn a confusing rustup error into a clear one.</p><h3 id="timeouts-and-retries" tabindex="-1">Timeouts and Retries <a class="header-anchor" href="#timeouts-and-retries" aria-label="Permalink to “Timeouts and Retries”">​</a></h3><p>Every rustup verb used here downloads from <code>static.rust-lang.org</code>. Each call is bounded by a timeout (10 minutes for rustup, 30 seconds for <code>rustc --version</code>) so a stalled connection cannot hold the job until the workflow&#39;s own limit, and network-bound commands are retried up to three times with 1s/2s backoff so a single dropped connection does not fail the run.</p><h3 id="relocatable-rustup-home" tabindex="-1">Relocatable <code>RUSTUP_HOME</code> <a class="header-anchor" href="#relocatable-rustup-home" aria-label="Permalink to “Relocatable RUSTUP_HOME”">​</a></h3><p><code>resolveRustupEnv</code> honours a caller-supplied <code>RUSTUP_HOME</code>/<code>CARGO_HOME</code> and only falls back to <code>$HOME/…</code>. This matters on overlayfs-backed container runtimes (Docker, <code>act</code>, container jobs): rustup renames a component&#39;s <em>directory</em> into <code>$RUSTUP_HOME/tmp</code> before replacing it, and overlayfs rejects renaming a directory that still lives in a lower image layer with <code>EXDEV</code>. Pointing <code>RUSTUP_HOME</code> at a directory created at run time keeps every rename inside one layer.</p><h2 id="tooling-stack" tabindex="-1">Tooling Stack <a class="header-anchor" href="#tooling-stack" aria-label="Permalink to “Tooling Stack”">​</a></h2>`,78),(s(),o(r,null,{default:t(()=>[i(g,{id:`mermaid-945`,graph:`flowchart%20LR%0A%20%20%20%20subgraph%20Runtime%0A%20%20%20%20%20%20%20%20BUN%5BBun%201.3%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Lang%0A%20%20%20%20%20%20%20%20TS%5BTypeScript%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Tooling%0A%20%20%20%20%20%20%20%20MISE%5Bmise%5D%0A%20%20%20%20%20%20%20%20LINT%5BESLint%2010.x%5D%0A%20%20%20%20%20%20%20%20FMT%5BPrettier%203.x%5D%0A%20%20%20%20%20%20%20%20HK%5Bhk%5D%0A%20%20%20%20%20%20%20%20COMMITLINT%5Bcommitlint%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20GitHub%0A%20%20%20%20%20%20%20%20ACTION%5Bactionlint%5D%0A%20%20%20%20%20%20%20%20ACT%5Bnektos%2Fact%5D%0A%20%20%20%20end%0A%0A%20%20%20%20BUN%20--%3E%20TS%0A%20%20%20%20MISE%20--%3E%20BUN%0A%20%20%20%20BUN%20--%3E%20LINT%0A%20%20%20%20BUN%20--%3E%20FMT%0A%20%20%20%20BUN%20--%3E%20COMMITLINT%0A%20%20%20%20HK%20--%3E%20BUN%0A%20%20%20%20ACT%20--%3E%20ACTION%0A`})]),_:1})),d[9]||=u(`<h3 id="runtime-dependencies" tabindex="-1">Runtime Dependencies <a class="header-anchor" href="#runtime-dependencies" aria-label="Permalink to “Runtime Dependencies”">​</a></h3><table tabindex="0"><thead><tr><th>Package</th><th>Purpose</th></tr></thead><tbody><tr><td><code>@actions/core</code></td><td>Inputs, outputs, <code>exportVariable</code>, <code>saveState</code>/<code>getState</code>, failures</td></tr><tr><td><code>@actions/cache</code></td><td><code>restoreCache</code>/<code>saveCache</code> behind the <code>CacheClient</code> port</td></tr><tr><td><code>smol-toml</code></td><td><code>rust-toolchain.toml</code> parsing</td></tr><tr><td><code>@actions/github</code></td><td>Declared for workflow <code>context</code>, but no module imports it today</td></tr></tbody></table><p><code>@actions/cache</code> is reached only through <code>CacheClient</code> (<code>src/cache/client.ts</code>), a type-only interface wired to the real SDK in <code>src/index.ts</code>. That indirection is why it costs nothing at test time and why it appears in exactly one import site. It is also the single largest contributor to the bundle: adding it took <code>dist/index.js</code> from roughly 789 KB to 2,209,190 bytes, a cost accepted deliberately in the Phase B design rather than reimplementing the cache protocol.</p><p><code>@actions/github</code> is declared in <code>package.json</code> but imported nowhere, so the bundler drops it — it contributes zero bytes to <code>dist/index.js</code>. It is kept because <code>AGENTS.md</code> directs contributors to it rather than to raw <code>process.env</code>/<code>GITHUB_*</code> access when workflow context is eventually needed.</p>`,4)])}var m=n(f,[[`render`,p]]);export{d as __pageData,m as default};